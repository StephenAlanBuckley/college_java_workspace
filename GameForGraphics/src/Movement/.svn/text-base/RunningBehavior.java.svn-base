package Movement;

import javax.media.opengl.awt.GLCanvas;

import main.Game;
import main.Player;

public class RunningBehavior extends WalkingBehavior{
	
	public RunningBehavior(Game game, GLCanvas canvas, Eye eye, Player player) 
	{
		super(game, canvas, eye,player);
		maxForwardSpeed = 8;
		forwardAcceleration = 1;
		maxStrafeSpeed = 5;
		strafeAcceleration = 1;
	}

}
