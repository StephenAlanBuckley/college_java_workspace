package Vehicles;
import java.util.List;

import javax.media.opengl.GL2;
import javax.media.opengl.awt.GLCanvas;
import javax.media.opengl.glu.GLU;

import main.Game;
import Movement.Eye;

public class VehicleSpawner {
	
	public static void spawnVehicles(List<Vehicle> vehicleList, Game game, GLCanvas canvas, Eye eye, GL2 gl, GLU glu){
		//vehicleList.add(new MetalGear(game, canvas, eye, gl, glu, 100, 0, 100));
		vehicleList.add(new GreenCar(game, canvas, eye, gl, glu, 100, 0, 100));
		vehicleList.add(new FireTruck(game, canvas, eye, gl, glu, 120, 0, 120));
	}
}
